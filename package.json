{
  "name": "eastcoastavl",
  "private": true,
  "scripts": {
    "install-dotnet": "curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --channel 10.0",
    "build": "npm run install-dotnet && ~/.dotnet/dotnet publish -c Release"
  }
}
